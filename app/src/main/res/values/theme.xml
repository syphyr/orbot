<?xml version="1.0" encoding="utf-8"?>

<resources>

    <style name="DefaultTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="colorPrimary">@color/new_background</item>
        <item name="android:windowBackground">@color/new_background</item>
        <item name="android:windowIsFloating">false</item>
        <item name="colorPrimaryDark">@color/new_background</item>
        <item name="preferenceTheme">@style/PrefsTheme</item>
        <item name="alertDialogTheme">@style/OrbotDialogTheme</item>
    </style>

    <style name="OrbotDialogTheme" parent="Base.Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="materialAlertDialogBodyTextStyle">@android:color/white</item>
        <item name="android:background">@drawable/bg_modal_rounded</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColorSecondary">@android:color/white</item>
        <item name="textAppearanceListItem">@android:color/white</item>
        <item name="android:layout_gravity">left</item>
        <item name="colorPrimary">@android:color/white</item>
        <item name="colorAccent">@android:color/white</item>
        <item name="textColorAlertDialogListItem">@android:color/white</item>
    </style>

</resources>

